//array é uma forma de voce agrupar multiplos valores a partir de uma 
//única estrutura, acessa os elementos a partir de um número. ex:

const valores = [7.7, 8.9, 6.3, 9.2, "esses são os valores"];
console.log(valores[0], valores[3]); // aqui eu estou acessando os numeros das
                                     // estruturas a partir de números.
console.log(valores);
console.log(valores.length); // é utilizado para mostrar quantos valores tem no array

console.log(valores.pop()); //mostra o ultimo elemento do array

